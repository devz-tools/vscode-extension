# Whitelist approach: Exclude everything by default, then include only what's needed
**

# Include compiled extension code
!dist/**

# Include LSP server binaries
!bin/**

# Include language support files
!syntaxes/**
!language-configuration.json

# Include documentation
!LICENSE
!README.md

# Include package manifest
!package.json

# Explicitly exclude source maps even from dist
dist/**/*.map